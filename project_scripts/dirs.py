# Standard library imports.
from pathlib import Path

# Project directories.
local_dir: Path = Path.cwd()
json_dir: Path = Path.cwd().parent / "json_files"
events_dir: Path = Path.cwd().parent / "event_files"